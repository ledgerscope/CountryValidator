namespace CountryValidation
{
    public enum ValidationStatus
    {
        NotSet,
        Valid,
        InvalidChecksum,
        InvalidFormat,
        InvalidDate,
        InvalidLength,
    }
}
